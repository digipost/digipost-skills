Add "fallback-on-error ≠ fallback-on-confirmed-non-user" as an explicit anti-pattern in the send-post skill. Routing to costly physical mail because identify threw is a distinct, dangerous behaviour worth calling out.
Elevate keystore handling from 🟡-announce to an actionable, verified step in auth-and-signing: gitignore the keystore glob, assert it isn't tracked, and rotate if ever committed. Mirror it in the eval rubric — the no_hardcoded_secrets check passes today because the secret is in a gitignored .env, yet the private key sits unignored beside it; add a "keystore is gitignored / not in history" assertion.
Let the entry batch infer likely data-types from the business domain so the 🔵 invoice offer actually fires (regnskapsbyrå → invoices/reminders). Right now the offer is real but nothing triggers it.
Record resolved decisions somewhere durable (a short DECISIONS block the agent writes), so drift like the inbox auto-delete-vs-manual contradiction is visible at the end instead of buried in a chat transcript.